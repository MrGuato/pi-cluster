apiVersion: v1
kind: Service
metadata:
  name: vikunja
spec:
  selector:
    app: vikunja    # Traffic goes to pods labeled app=vikunja
  ports:
    - name: http
      port: 3456
      targetPort: 3456
  type: ClusterIP   # Only reachable inside the cluster — Cloudflare tunnel handles external access
